{
	"name": "@spysystem/jquery-zoom",
	"description": "Enlarge images on click or mouseover.",
	"version": "1.7.24",
	"keywords": [
		"zoom",
		"images",
		"ui",
		"jquery-plugin"
	],
	"scripts": {
		"compile": "uglifyjs -c --comments '/license:/' jquery.zoom.js -o jquery.zoom.min.js",
		"prepack": "tsc -p tsconfig.json && yarn run compile"
	},
	"main": "jquery.zoom.min.js",
	"license": "MIT",
	"dependencies": {
		"@types/jquery": ">=1.7",
		"jquery": ">=1.7"
	},
	"devDependencies": {
		"typescript": "^4.6.3",
		"uglify-js": "^3.15.3"
	}
}
